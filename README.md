# Rohan — GitHub Pages Portfolio

A fast, responsive, and professional single-page portfolio for **Rohan** (Flutter Developer • Social Media Expert • Content Writer).

## 🚀 Quick Start (GitHub Pages)
1. Create a new repository named **rohan** or **rohan.github.io** (recommended to use `your-username.github.io` for user site).
2. Upload all files from this folder to the repository root.
3. Go to **Settings → Pages** and ensure the source is set to **Deploy from a branch**, branch **main**, folder **/** (root).
4. Your site will be live at `https://your-username.github.io/` after the first build.

## ✏️ Customize
- **Name & links**: Edit `index.html` (email, Instagram, GitHub username).
- **Projects**: Edit `projects.json` — update titles, tags, links, and images.
- **Colors & styles**: Tweak CSS variables in `assets/css/style.css`.
- **QR**: Replace `assets/img/instagram-qr-placeholder.svg` with your real QR file.
- **Resume**: Replace `resume/rohan-resume.pdf` with your actual résumé PDF.
- **Form**: Replace the `action` in the contact form with your Formspree endpoint.

## 🌓 Theme
Dark/Light/System toggle is built-in. Choice is saved in `localStorage`.

## ♿ Accessibility
- Skip link for keyboard users.
- Sufficient color contrast.
- Semantic landmarks and labels.

## 🧱 Stack
- Vanilla HTML/CSS/JS (no build step).
- Lightweight and super fast.

---
Built on 2025-11-01.
